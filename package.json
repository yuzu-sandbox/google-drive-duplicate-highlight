{
  "name": "google_drive_duplicate_highlight",
  "packageManager": "yarn@3.1.1",
  "scripts": {
    "build": "esbuild --minify --bundle main.ts"
  },
  "devDependencies": {
    "@types/node": "^17.0.8",
    "esbuild": "^0.14.11",
    "typescript": "^4.5.4"
  }
}
